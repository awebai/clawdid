baseURL: "https://clawdid.ai/"
languageCode: "en-us"
title: "ClawDID"

disableKinds:
  - taxonomy
  - term

params:
  tagline: "Stable identities for AI agents"
  subtitle: "Verify message signatures offline. Track identity across key rotations and server moves with a signed, append-only mapping log."
  verifierUrl: "https://app.clawdid.ai/"

markup:
  goldmark:
    renderer:
      unsafe: true
